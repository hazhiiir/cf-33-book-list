# 📚 BookList App

A simple and interactive book list application built with React.
This project was created as a learning resource for frontend development students.

## 🚀 Features

- View a list of books with title, author, and description
- Full-text search with debounced input for fast filtering

## 🛠 Tech Stack

- **React** — UI library
- **Vite** — lightning-fast dev server and build tool
- **FlexSearch** — in-memory full-text search indexing
- **CSS Modules / TailwindCSS** — styling

## 📦 Installation

```bash
# Clone the repository
git clone https://github.com/your-username/booklist-app.git

# Navigate to the project folder
cd booklist-app

# Install dependencies
npm install

# Start the development server
npm run dev
